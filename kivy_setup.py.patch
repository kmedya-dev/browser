--- a/setup.py
+++ b/setup.py
@@ -215,6 +215,11 @@
 
 platform = sys.platform
+if 'DROIDBUILDER_ANDROID_BUILD' in environ:
+    platform = 'android'
+    ndkplatform = environ.get('NDKPLATFORM')
+
+
 c_options = {
     'use_rpi': platform == 'rpi',
     'use_mali': platform == 'mali',
