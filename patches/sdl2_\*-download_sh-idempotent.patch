--- a/external/download.sh
+++ b/external/download.sh
@@ -19,5 +19,8 @@
         ARGUMENTS="--depth 1"
     fi
 
-    git clone --filter=blob:none $url $path -b $branch --recursive $ARGUMENTS
+    if [ -d "$path" ]; then
+        echo "Submodule directory $path already exists, skipping clone."
+    else
+        git clone --filter=blob:none $url $path -b $branch --recursive $ARGUMENTS
+    fi
 done
